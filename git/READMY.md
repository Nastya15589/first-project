# Маленький перечень команд в git
---


Сначала нужно  **инициализировать** репозиторий - ``` git init```
Теперь добавляем наши _измененные_ файлы = ``` git add --all или git add name_file```
** Обязательно** нужно не забыть добавить коммит с описанием нашей проделаной работы - ``` git commit -m 'Мы молодцы!'```
Для просмотра истории логов используем ``` git log```
Чтобы синхронизировать локальный и удаленный сервер - ```git push -u origin main```